# todo: add batching

name: "MODEL_SE_NAME"
backend: "python"

input [
  {
    name: "AUDIO_SIGNAL_INPUT_NAME"
    data_type: TYPE_FP32
    dims: [ -1 ]
  }
]

output [
  {
    name: "SPEAKER_EMBEDDING_OUTPUT_NAME"
    data_type: TYPE_FP32
    dims: [ -1 ]
  }
]

instance_group [
  {
      count: 1
      kind: KIND_GPU
      gpus: [ 0 ]
    }
]
